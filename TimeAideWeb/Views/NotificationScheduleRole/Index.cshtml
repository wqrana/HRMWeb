@model IEnumerable<TimeAide.Web.Models.NotificationScheduleRole>

<div class="form-group row">
    <div class="col-md-12">
        <table class="table table-striped custom-table table-nowrap mb-0" id="tblUser">
            <thead>
                <tr>
                    <th>
                        Notification Role Id
                    </th>
                    <th>
                        Notification Role Name
                    </th>
                    <th>
                        Description
                    </th>
                </tr>
            </thead>
            <tbody>
                @if (Model != null)
                {
                    foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.Id)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.NotificationRole.NotificationRoleName)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.NotificationRole.NotificationRoleDescription)
                            </td>
                        </tr>
                    }
                }
            </tbody>
        </table>
    </div>
</div>
