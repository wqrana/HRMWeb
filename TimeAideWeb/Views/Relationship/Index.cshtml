@model IEnumerable<TimeAide.Web.Models.Relationship>

@Html.Partial("_IndexPageHeaderView")
<thead>
    <tr>
        <th>
            Relationship
        </th>
        <th>
            Description
        </th>
        <th></th>
    </tr>
</thead>
<tbody>
    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.RelationshipName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RelationshipDescription)
            </td>
            <td>
                @Html.Partial("_IndexPageButtonsView", item)
            </td>
        </tr>
    }

</tbody>
@Html.Partial("_IndexPageFooterView")
