@model IEnumerable<TimeAide.Web.Models.TerminationEligibility>

@Html.Partial("_IndexPageHeaderView")
<thead>
    <tr>
        <th>
            Termination Eligibility
        </th>
        <th>
            Description
        </th>
        <th></th>
    </tr>
</thead>
<tbody>
    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TerminationEligibilityName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TerminationEligibilityDescription)
            </td>
            <td>
                @Html.Partial("_IndexPageButtonsView", item)
            </td>
        </tr>
    }
</tbody>
@Html.Partial("_IndexPageFooterView")
