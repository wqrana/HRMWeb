@model IEnumerable<TimeAide.Web.Models.TerminationType>

@Html.Partial("_IndexPageHeaderView")
<thead>
    <tr>
        <th>
            Termination Type
        </th>
        <th>
            Description
        </th>

        <th></th>
    </tr>
</thead>
<tbody>
    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TerminationTypeName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TerminationTypeDescription)
            </td>
            <td>
                @Html.Partial("_IndexPageButtonsView", item)
            </td>
        </tr>
    }
</tbody>
@Html.Partial("_IndexPageFooterView")
