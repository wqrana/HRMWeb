@model IEnumerable<TimeAide.Web.Models.AppraisalTemplateSkill>
<div class="table-responsive">
    <table class="table table-striped custom-table table-nowrap mb-0">
        <tr>
            <th>
                Skill Name
            </th>

        </tr>
        @if (Model!= null)
        {
            foreach (var item in Model)
            {
                <tr>

                    <td>@item.AppraisalSkill.SkillName</td>

                </tr>
            }
        }
    </table>
</div>
