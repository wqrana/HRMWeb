@model IEnumerable<TimeAide.Web.Models.EmployeeCredential>

<div id="CredentialUpoadHistory_modal" class="modal custom-modal">
    <div class="modal-dialog modal-dialog-centered modal-lg">
        <div class="modal-content">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title">Credential upoad History for @ViewBag.CredentialTitle</h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">×</span>
                    </button>
                </div>
                <div class="modal-body">
                    <div class="row">
                        <div class="col-md-12">
                            <div class="table-responsive">
                                <table class="table  table-striped custom-table table-nowrap mb-0" style="width:100% !important">
                                    <thead>
                                        <tr>
                                            <th>File Name</th>
                                            <th>Upload Date</th>
                                            <th>Download</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        @foreach (var eachEmployeeCredential in Model)
                                        {
                                        <tr>

                                            <td>@eachEmployeeCredential.DocumentName</td>
                                            <td>@eachEmployeeCredential.CreatedDate</td>
                                            <td><a href="javascript:downloadCredential(@eachEmployeeCredential.Id)" class="dropdown-item"><i class="fa fa-download m-r-5"></i> Download Credential</a></td>
                                        </tr>
                                        }
                                    </tbody>
                                </table>
                            </div>
                        </div>
                    </div>
                    <div class="submit-section mt-1">

                        <button id="btnCancel" class="btn btn-primary  submit-btn" data-dismiss="modal" aria-label="Close">Closed</button>
                    </div>

                </div>
            </div>
        </div>
    </div>
</div>
